<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springlec.base.dao.UserDao">

	<select id="userInfo" resultType="com.springlec.base.model.User">
		SELECT * FROM user u, userAddress ad
	</select>
	
	<insert id="writeDao">
		INSERT INTO addressInfo(aName, aPhone, aAddress, aEmail, aRelation) VALUES(#{aName}, #{aPhone}, #{aAddress}, #{aEmail}, #{aRelation})
	</insert>

	<select id="viewDao" resultType="com.springlec.base.model.User">
		SELECT * FROM addressInfo WHERE aNo = #{aNo}
	</select>

</mapper>